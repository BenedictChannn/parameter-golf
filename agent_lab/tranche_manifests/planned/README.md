# Planned Tranche Manifests

These manifests capture the next research campaigns before their code support exists.

Use them for:

- concrete experiment design
- preserving hypotheses, goals, and candidate structure
- implementation planning for future architecture or optimizer work

Do not execute them with `run_tranche.py` until the referenced env knobs or code paths have been implemented in [`agent_lab/train_gpt.py`](../../train_gpt.py).
