# 2026-03-31 Agent Lab

## Objective

Close out the overnight `J` to `O` program honestly, update the tracked research memory, and decide what the lab should care about next.

## What Actually Finished

- `J` hybrid sequence mixer: all 5 completed
- `K` output head architecture: all 5 completed
- `L` local-global attention: only `L1` ran, and it crashed
- `M` skip/residual redesign: all 5 completed
- `N` mechanism-specific learning rates: all 5 completed
- `O` quantization-aware warmdown: all 5 completed

Important harness note:
- the top-level program continued after the `L1` crash because `run_tranche.py` currently records a crashed candidate and then exits cleanly rather than failing the tranche with a nonzero exit code
- that means the overnight program did not behave like “stop on tranche failure”; it behaved like “mark the tranche crash and continue to the next tranche”

That is not a research result, it is harness behavior. It needs to be remembered when reading the overnight run as one clean chain.

## Best New Result

- New best valid run: [`AL-20260330-104`](../../agent_lab/experiments.tsv)
- Family: hybrid sequence mixer
- Shape: lower four attention layers replaced by sequence mixers
- `final_int8_ttt_lora val_bpb`: `1.3488`
- Artifact: `14,717,182` bytes int8+zlib

Simple interpretation:
- this was the real breakthrough of the overnight program
- the next frontier move came from changing the block, not from another small scalar retune
- the lower stack turned out to be the right place to save attention cost

## Tranche Reads

### J - Hybrid Sequence Mixer

- `J1` and `J2` both lost, so “replace the middle” was not the right generic story
- `J3` alternating attention and mixers worked and nearly beat the old frontier
- `J4` lower four mixer layers won hard and became the new best run
- `J5` upper four mixer layers stayed respectable, but was clearly weaker than lower-stack replacement

What I think:
- the family is real
- the placement matters a lot
- the early stack is where full attention was most overbuilt

### K - Output Head Architecture

- low-rank heads were bad
- bottleneck and two-stage nonlinear heads were also bad
- tied-plus-residual was the only version that looked at all plausible, but it was still clearly behind the dense untied head

What I think:
- tranche F was already telling the truth
- the win was the dense untied head plus calibration, not some hidden “better head architecture”
- this family is not where I would spend the next tranche on the current backbone

### L - Local-Global Attention

- only `L1` ran
- it crashed before the first metric

What I think:
- this family is unresolved, not disproven
- I do not want to learn the wrong lesson from a crash
- it needs a real debug pass before any scientific judgment

### M - Skip And Residual Redesign

- shared scalar skip gates alone already improved over the old pre-program best
- sparse skip topology gave that gain back
- scalar `resid_mix` and shared residual scales stayed close but not best
- the full cheap-routing package stacked into `1.3534`, which is the second-best overnight result

What I think:
- tranche H was right that deletion was the wrong question
- the family wanted redesign, not removal
- this line is strong enough that it deserves a combo test with the hybrid-mixer winner

### N - Mechanism-Specific Learning Rates

- attention-up / MLP-down was the strongest subsystem split
- early-layer boost also looked slightly better than late-layer boost
- new-mechanism LR boost stayed near the old frontier
- none of them touched the new hybrid-mixer best

What I think:
- these are support tools
- they may matter more when paired with a promising new architecture than as a standalone frontier family

### O - Quantization-Aware Warmdown

- longer and earlier warmdown both hurt badly
- a short stabilization phase was only a near miss
- head-only cooldown almost tied the old best, but still lost
- the combined quant-tail also failed badly

What I think:
- the obvious “cool the model more before export” story is wrong
- if there is something real here, it is probably more output-path-specific and much subtler than the first pass

## Durable Conclusions

- The overnight winner was architectural: hybrid mixers in the lower stack.
- The runner proved that bold block changes can beat the current transformer frontier on this stack.
- Skip/residual redesign is the best secondary family from the overnight program.
- Output-head architecture and naive quantization-aware warmdown are both de-prioritized for now.
- Local-global attention is still unfinished because the first candidate crashed.

## Next Moves

The three strongest next directions are:

- refine the hybrid-mixer winner around [`AL-20260330-104`](../../agent_lab/experiments.tsv)
- test whether the cheap-routing package from [`AL-20260330-120`](../../agent_lab/experiments.tsv) stacks with the hybrid-mixer winner
- debug the local-attention crash and only then decide whether local-global attention deserves more compute

## Validation And Artifacts

- tracked runtime summaries: [`agent_lab/tranche_runs/`](../../agent_lab/tranche_runs)
- tracked experiment ledger: [`agent_lab/experiments.tsv`](../../agent_lab/experiments.tsv)
- runtime ledger: [`agent_lab/results.tsv`](../../agent_lab/results.tsv)
- plots refreshed: [`agent_lab/plots/experiments.html`](../../agent_lab/plots/experiments.html)
- budget note: the current `analyze_budget.py` helper is still transformer-only and does not understand mixer layers, so I did not overwrite the budget snapshot with a misleading report for the new hybrid frontier

## Raw Note

This was the first overnight block in a while that actually felt like research instead of gardening. The biggest thing I learned is that the model was willing to give up a surprising amount of early attention if it got a cheaper sequence mechanism in return. That is a much more interesting answer than another `0.0002` from tuning `HEAD_LR`. The other thing I do not want to forget is that the local-attention tranche did not fail scientifically; it failed operationally. If I let myself mentally file that as “local windows lost,” I would be lying to future me.
